spring:
  application:
    name: shop
  cloud:
    config:
      enabled: true
      name: shop
      profile: dev
      label: master
      uri: http://localhost:8888/
      failFast: true
      retry:
        initialInterval: 1000
        maxInterval: 90000
        maxAttempts: 5
